$(document).ready(function() {

    /* $("main").css({
        "top": -$("nav").outerHeight(true),
        "margin-bottom": -$("nav").outerHeight(true)
    }); */

    /*$(window).scroll(function() {
        if($(window).scrollTop() > $("header").offset().top + $("header").outerHeight()) {
            $("nav").addClass("view");
        }
        else{
            $("nav").removeClass("view");
        }
    });*/
});